# okr-ai-coach

IA coach (Edge Function): valida un objetivo+KRs en borrador y devuelve feedback (tarea-disfrazada, KR sin numero, foco). Claude via Edge Function (key server-side). Asesora, no genera.
